{
  "name": "game-identity",
  "description": "Monorepo for gamer identity on holochain",
  "scripts": {
    "test": "npm run build:happ && cargo nextest run -j 1",
    "build:zomes": "RUSTFLAGS='' CARGO_TARGET_DIR=target cargo build --release --target wasm32-unknown-unknown --package username_registry_integrity --package username_registry",
    "build:happ": "npm run build:zomes && hc app pack workdir --recursive"
  }
}
