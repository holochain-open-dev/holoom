{
  "name": "game-identity",
  "description": "Monorepo for gamer identity on holochain",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:zomes": "RUSTFLAGS='' CARGO_TARGET_DIR=target cargo build --release --target wasm32-unknown-unknown --package game_identity_integrity --package game_identity_coordinator",
    "build:happ": "npm run build:zomes && hc app pack workdir --recursive"
  }
}
