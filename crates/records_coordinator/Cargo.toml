[package]
name = "records_coordinator"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
name = "records_coordinator"

[dependencies]
hdk = { workspace = true }
serde = { workspace = true }
